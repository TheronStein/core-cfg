- `q{register}` → Start recording macro in register `{register}`
    
- `q` → Stop recording
    
- `@{register}` → Play macro stored in `{register}`
    
- `@@` → Repeat last used macro
    
- `:registers` → View contents of registers
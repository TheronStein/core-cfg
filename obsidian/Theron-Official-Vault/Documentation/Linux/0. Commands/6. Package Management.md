- `apt-get update`: Update the package lists.
- `apt-get upgrade`: Upgrade all packages.
- `apt-get install`: Install new packages.
- `apt-get remove`: Remove packages.
- `apt-cache search`: Search for a package in the repository.
- `dpkg -i`: Install a package manually using a `.deb` file.
- `snap install`: Install applications via snap packages.
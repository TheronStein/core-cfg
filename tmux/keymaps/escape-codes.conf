unbind -n M-Enter
# unbind -n C-`
unbind -n C-2
unbind -n C-i
unbind-key -n C-`
# unbind-key -n C-i
unbind  t
unbind '"'
unbind %
unbind R

# Ctrl+Key combinations that conflict with ASCII control codes
bind-key -n C-i send-keys -l "\e[105;5u"
bind-key -n C-j send-keys -l "\e[106;5u"
bind-key -n C-k send-keys -l "\e[107;5u"
bind-key -n C-l send-keys -l "\e[108;5u"
bind-key -n C-m send-keys -l "\e[109;5u"
bind-key -n C-h send-keys -l "\e[104;5u"

# Ctrl+Shift combinations
bind-key -n C-S-w send-keys -l "\e[119;6u"
bind-key -n C-S-s send-keys -l "\e[115;6u"
bind-key -n C-S-a send-keys -l "\e[97;6u"
bind-key -n C-S-d send-keys -l "\e[100;6u"
bind-key -n C-S-q send-keys -l "\e[113;6u"
bind-key -n C-S-e send-keys -l "\e[101;6u"
bind-key -n C-S-i send-keys -l "\e[105;6u"
bind-key -n C-S-j send-keys -l "\e[106;6u"
bind-key -n C-S-k send-keys -l "\e[107;6u"
bind-key -n C-S-l send-keys -l "\e[108;6u"
bind-key -n C-S-u send-keys -l "\e[117;6u"
bind-key -n C-S-o send-keys -l "\e[111;6u"

# Ctrl+Alt combinations
bind-key -n C-M-w send-keys -l "\e[119;7u"
bind-key -n C-M-s send-keys -l "\e[115;7u"
bind-key -n C-M-a send-keys -l "\e[97;7u"
bind-key -n C-M-d send-keys -l "\e[100;7u"
bind-key -n C-M-q send-keys -l "\e[113;7u"
bind-key -n C-M-e send-keys -l "\e[101;7u"
bind-key -n C-M-i send-keys -l "\e[105;7u"
bind-key -n C-M-j send-keys -l "\e[106;7u"
bind-key -n C-M-k send-keys -l "\e[107;7u"
bind-key -n C-M-l send-keys -l "\e[108;7u"
bind-key -n C-M-u send-keys -l "\e[117;7u"
bind-key -n C-M-o send-keys -l "\e[111;7u"

# Alt+Shift combinations
bind-key -n M-S-w send-keys -l "\e[119;4u"
bind-key -n M-S-s send-keys -l "\e[115;4u"
bind-key -n M-S-a send-keys -l "\e[97;4u"
bind-key -n M-S-d send-keys -l "\e[100;4u"
bind-key -n M-S-q send-keys -l "\e[113;4u"
bind-key -n M-S-e send-keys -l "\e[101;4u"

# Tab variants
bind-key -n C-Tab send-keys -l "\e[9;5u"
bind-key -n C-S-Tab send-keys -l "\e[9;6u"
bind-key -n C-M-Tab send-keys -l "\e[9;7u"
bind-key -n M-S-Tab send-keys -l "\e[9;4u"

# Enter variants
bind-key -n C-Enter send-keys -l "\e[13;5u"
bind-key -n M-Enter send-keys -l "\e[13;3u"
bind-key -n C-M-Enter send-keys -l "\e[13;7u"
bind-key -n C-S-Enter send-keys -l "\e[13;6u"
# bind-key -n S-Enter send-keys -l "\e[13;2u"

# Space variants
bind-key -n C-Space send-keys -l "\e[32;5u"
bind-key -n M-Space send-keys -l "\e[32;3u"
bind-key -n C-M-Space send-keys -l "\e[32;7u"
bind-key -n C-S-Space send-keys -l "\e[32;6u"

# Backtick
bind-key -n C-` send-keys -l "\e[96;5u"
bind-key -n M-` send-keys -l "\e[96;3u"

# Bracket keys
bind-key -n C-S-[ send-keys -l "\e[91;6u"
bind-key -n C-S-] send-keys -l "\e[93;6u"

# Minus/Equal
bind-key -n C-S-- send-keys -l "\e[45;6u"
bind-key -n C-S-= send-keys -l "\e[61;6u"
bind-key -n M-- send-keys -l "\e[45;3u"
bind-key -n M-= send-keys -l "\e[61;3u"

# Number keys with Ctrl
bind-key -n C-0 send-keys -l "\e[48;5u"
bind-key -n C-1 send-keys -l "\e[49;5u"
bind-key -n C-2 send-keys -l "\e[50;5u"
bind-key -n C-3 send-keys -l "\e[51;5u"
bind-key -n C-4 send-keys -l "\e[52;5u"
bind-key -n C-5 send-keys -l "\e[53;5u"
bind-key -n C-6 send-keys -l "\e[54;5u"
bind-key -n C-7 send-keys -l "\e[55;5u"
bind-key -n C-8 send-keys -l "\e[56;5u"
bind-key -n C-9 send-keys -l "\e[57;5u"

# Number keys with Ctrl+Shift
bind-key -n C-S-0 send-keys -l "\e[48;6u"
bind-key -n C-S-1 send-keys -l "\e[49;6u"
bind-key -n C-S-2 send-keys -l "\e[50;6u"
bind-key -n C-S-3 send-keys -l "\e[51;6u"
bind-key -n C-S-4 send-keys -l "\e[52;6u"
bind-key -n C-S-5 send-keys -l "\e[53;6u"
bind-key -n C-S-6 send-keys -l "\e[54;6u"
bind-key -n C-S-7 send-keys -l "\e[55;6u"
bind-key -n C-S-8 send-keys -l "\e[56;6u"
bind-key -n C-S-9 send-keys -l "\e[57;6u"

# Number keys with Alt
bind-key -n M-0 send-keys -l "\e[48;3u"
bind-key -n M-1 send-keys -l "\e[49;3u"
bind-key -n M-2 send-keys -l "\e[50;3u"
bind-key -n M-3 send-keys -l "\e[51;3u"
bind-key -n M-4 send-keys -l "\e[52;3u"
bind-key -n M-5 send-keys -l "\e[53;3u"
bind-key -n M-6 send-keys -l "\e[54;3u"
bind-key -n M-7 send-keys -l "\e[55;3u"
bind-key -n M-8 send-keys -l "\e[56;3u"
bind-key -n M-9 send-keys -l "\e[57;3u"

# Shift+Tab (most common problem - often captured by tmux/terminal)
bind-key -n S-Tab send-keys -l "\e[Z"

# Shift+Enter
bind-key -n S-Enter send-keys -l "\e\r"
# bind-key -n S-Enter send-keys -l "\e[13;2u"

# Shift+Space (if you need to distinguish it from regular space)
bind-key -n S-Space send-keys -l "\e[32;2u"

# Shift+Backspace
bind-key -n S-BSpace send-keys -l "\e[127;2u"

# Shift+Escape
bind-key -n S-Escape send-keys -l "\e[27;2u"

# Shift+Delete
bind-key -n S-DC send-keys -l "\e[3;2~"

# Shift+Insert
bind-key -n S-IC send-keys -l "\e[2;2~"

# Shift+Home
bind-key -n S-Home send-keys -l "\e[1;2H"

# Shift+End
bind-key -n S-End send-keys -l "\e[1;2F"

# Shift+PageUp
bind-key -n S-PPage send-keys -l "\e[5;2~"

# Shift+PageDown
bind-key -n S-NPage send-keys -l "\e[6;2~"

#### NIGHTMARE OVER SECTION ####

# set -g prefix2 C-F11
# bind C-F11 send C-F11
# unbind-key C-i

# Clear conflicting bindings
# unbind-key -T root C-`
# unbind-key -T prefix C-`
# unbind-key -n C-2
# unbind-key -T root C-2
# unbind-key -T prefix C-2
# 1. Unbind the default bind for C-i (Tab) to ensure it doesn't perform a tmux command
# This might not be strictly necessary if C-i is only being passed through,
# but it's good practice for custom keybinds.
# CRITICAL: Pass through Ctrl+i to the underlying application
# Without this, tmux will still treat Ctrl+i as Tab even with extended-keys
# bind -n C-i send-keys C-i

# bind -n C-i send-keys C-i

# Unbind M-Enter to prevent accidental menu popup (Shift+Enter triggers this)

# bind -n M-Enter 



# vim: ft=tmux:et:sw=0:ts=2:sts=2:fdm=marker:fmr=[[[,]]]:

───────────────────────────────────────────────────────────────────────────────────────────────

>  can you make me tmux popup window using the browser/preview bash scripts as a template in
> ~/.core/cfg/tmux/scripts/popups that serves as a list of configuration files, when i select
> the file, it should get the active wezterm gui and trigger a custom user event that splits a
> pane using wezterm to the right with the file open in neovim.
> v

# ============================================================================
# KEYBOARD SHORTCUTS
# ============================================================================
# Clear all default shortcuts first
clear_all_shortcuts yes

# Kitty mod key - this is your main modifier
kitty_mod ctrl+Shift

keyboard_protocol kitty
map kitty_mod+r load_config_file

# ============================================================================
# CLIPBOARD OPERATIONS
# ============================================================================
map kitty_mod+c copy_to_clipboard
map kitty_mod+v paste_from_clipboard
map kitty_mod+s paste_from_selection
map shift+insert paste_from_selection
map kitty_mod+o pass_selection_to_program
map ctrl+e toggle_marker text 1 ERROR

# # ============================================================================
# # CLIPBOARD OPERATIONS
# # ============================================================================
# map kitty_mod+c copy_to_clipboard
# map kitty_mod+v paste_from_clipboard
# map kitty_mod+s paste_from_selection
# map shift+insert paste_from_selection
# map kitty_mod+o pass_selection_to_program
# map ctrl+e toggle_marker text 1 ERROR
#

# ============================================================================
# TAB MANAGEMENT
# ============================================================================
# Create/close tabs
map kitty_mod+t new_tab
map kitty_mod+q previous_tab

# Navigate tabs

map ctrl+alt+1 goto_tab 1
map ctrl+alt+2 goto_tab 2
map ctrl+alt+3 goto_tab 3
map ctrl+alt+4 goto_tab 4
map ctrl+alt+5 goto_tab 5
map ctrl+alt+6 goto_tab 6
map ctrl+alt+7 goto_tab 7
map ctrl+alt+8 goto_tab 8
map ctrl+alt+9 goto_tab 9

# Move tabs
map kitty_mod+. move_tab_forward
map kitty_mod+, move_tab_backward

# Move focus in directions
map kitty_mod+j neighboring_window left
map kitty_mod+l neighboring_window right
map kitty_mod+i neighboring_window up
map kitty_mod+k neikghboring_window down
# Cycle through windows
map kitty_mod+] next_window
map kitty_mod+[ previous_window
# Move windows in directions
map alt+shift+j move_window left
map alt+shift+l move_window right
map alt+shift+i move_window up
map alt+shift+k move_window down

# ============================================================================
# OS SPECIFIC TWEAKS
# ============================================================================
wayland_titlebar_color system
linux_display_server auto

# ============================================================================
# KEYBOARD SHORTCUTS
# ============================================================================
# Clear all default shortcuts first
clear_all_shortcuts yes

# Kitty mod key - this is your main modifier
kitty_mod ctrl+shift

# ============================================================================
# SCROLLING
# ============================================================================
map kitty_mod+up scroll_line_up
map kitty_mod+k scroll_line_up
map kitty_mod+down scroll_line_down
map kitty_mod+j scroll_line_down
map kitty_mod+page_up scroll_page_up
map kitty_mod+page_down scroll_page_down
map kitty_mod+home scroll_home
map kitty_mod+end scroll_end
map kitty_mod+z scroll_to_prompt -1
map kitty_mod+x scroll_to_prompt 1
map kitty_mod+h show_scrollback
map kitty_mod+g show_last_command_output

# ============================================================================
# WINDOW MANAGEMENT (The Pane/Splitting Features)
# ============================================================================
# Create new windows (panes)
# map kitty_mod+enter new_window
map kitty_mod+n new_window
map ctrl+alt+enter new_window_with_cwd

# Close window
map kitty_mod+w close_window

# Navigate windows
map kitty_mod+] next_window
map kitty_mod+[ previous_window
map kitty_mod+f move_window_forward
map kitty_mod+b move_window_backward
map kitty_mod+` move_window_to_top
map kitty_mod+r start_resizing_window
map kitty_mod+1 first_window
map kitty_mod+2 second_window
map kitty_mod+3 third_window
map kitty_mod+4 fourth_window
map kitty_mod+5 fifth_window
map kitty_mod+6 sixth_window
map kitty_mod+7 seventh_window
map kitty_mod+8 eighth_window
map kitty_mod+9 ninth_window
map kitty_mod+0 tenth_window

# Focus specific window by number
map kitty_mod+f7 focus_visible_window

# Swap windows
map kitty_mod+ctrl+f move_window_forward
map kitty_mod+ctrl+b move_window_backward

# ============================================================================
# TAB MANAGEMENT
# ============================================================================
# Create/close tabs
map kitty_mod+t new_tab
# map kitty_mod+q close_tab
map kitty_mod+q previous_tab
map kitty_mod+e next_tab 
# map kitty_mod+alt+set_tab_title

# Navigate tabs
map kitty_mod+right next_tab
map kitty_mod+left previous_tab
map kitty_mod+alt+t set_tab_title
map ctrl+alt+1 goto_tab 1
map ctrl+alt+2 goto_tab 2
map ctrl+alt+3 goto_tab 3
map ctrl+alt+4 goto_tab 4
map ctrl+alt+5 goto_tab 5
map ctrl+alt+6 goto_tab 6
map ctrl+alt+7 goto_tab 7
map ctrl+alt+8 goto_tab 8
map ctrl+alt+9 goto_tab 9

# Move tabs
map kitty_mod+alt+q, move_tab_backward
map kitty_mod+alt+e. move_tab_forward

# ============================================================================
# LAYOUT MANAGEMENT (Test Different Layouts!)
# ============================================================================
map kitty_mod+l next_layout
map kitty_mod+alt+1 goto_layout splits
map kitty_mod+alt+2 goto_layout tall
map kitty_mod+alt+3 goto_layout fat
map kitty_mod+alt+4 goto_layout grid
map kitty_mod+alt+5 goto_layout horizontal
map kitty_mod+alt+6 goto_layout vertical
map kitty_mod+alt+7 goto_layout stack

# ============================================================================
# FONT SIZES
# ============================================================================
map kitty_mod+equal change_font_size all +2.0
map kitty_mod+plus change_font_size current +2.0
map kitty_mod+kp_add change_font_size all +2.0
map kitty_mod+minus change_font_size current -2.0
map kitty_mod+kp_subtract change_font_size all -2.0
map kitty_mod+backspace change_font_size all 0

# ============================================================================
# SELECT AND ACT ON VISIBLE TEXT (Hints Kitten - Very Powerful!)
# ============================================================================
# Open URL
# map kitty_mod+e open_url_with_hints

# Insert selected path
map kitty_mod+p>f kitten hints --type path --program -

# Open selected path
map kitty_mod+p>shift+f kitten hints --type path

# Insert selected line
map kitty_mod+p>l kitten hints --type line --program -

# Insert selected word
map kitty_mod+p>w kitten hints --type word --program -

# Insert selected hash
map kitty_mod+p>h kitten hints --type hash --program -

# Open selected hyperlink
map kitty_mod+p>n kitten hints --type hyperlink

# ============================================================================
# KITTENS - Special Features
# ============================================================================
# Unicode input
map kitty_mod+u kitten unicode_input

# Edit config
map kitty_mod+f2 edit_config_file

# Open kitty shell (REPL for controlling kitty)
map kitty_mod+escape kitty_shell window

# Themes browser (browse and apply themes interactively!)
map kitty_mod+f12 kitten themes

# Reload config
map kitty_mod+r load_config_file

# Debug config
map kitty_mod+f6 debug_config

# ============================================================================
# MISCELLANEOUS
# ============================================================================
map kitty_mod+a>m set_background_opacity +0.1
map kitty_mod+a>l set_background_opacity -0.1
map kitty_mod+a>1 set_background_opacity 1
map kitty_mod+a>d set_background_opacity default
map kitty_mod+delete clear_terminal reset active

# Toggle fullscreen
map kitty_mod+f11 toggle_fullscreen

# Toggle maximized
map kitty_mod+f10 toggle_maximized

# Show kitty docs
map kitty_mod+f1 show_kitty_doc overview

# ============================================================================
# CUSTOM KEY SEQUENCES FOR TESTING
# ============================================================================
# These demonstrate Kitty's ability to create custom key sequences

# Open new window with specific working directory
map ctrl+alt+n new_window_with_cwd

# Launch specific programs in new windows
map kitty_mod+f3 launch --cwd=current --type=tab
map kitty_mod+f4 launch --cwd=current --type=window

# ============================================================================
# SHELL INTEGRATION FEATURES
# ============================================================================
# These require shell integration to be set up
# Jump to previous/next shell prompt
map kitty_mod+ctrl+z scroll_to_prompt -1
map kitty_mod+ctrl+x scroll_to_prompt 1

# Browse last command output in pager
map kitty_mod+ctrl+g show_last_command_output

# ============================================================================
# REMOTE CONTROL (for scripting Kitty)
# ============================================================================
# Enabled above with: allow_remote_control yes
# You can control kitty with: kitty @ <command>
# Examples:
#   kitty @ set-spacing margin=20
#   kitty @ set-background-opacity 0.5
#   kitty @ launch --type=tab
#   kitty @ send-text "echo hello\n"

# ============================================================================
# ADDITIONAL FEATURES TO TEST
# ============================================================================
# 1. Image display: 
#    - kitty +kitten icat /path/to/image.png
#
# 2. Diff viewer:
#    - kitty +kitten diff file1.txt file2.txt
#
# 3. Clipboard over SSH:
#    - kitty +kitten ssh hostname
#    - Then copy/paste works over SSH!
#
# 4. Panel kitten:
#    - kitty +kitten panel -- <command>
#
# 5. Broadcast input to all windows:
#    - kitty @ send-text --match all "echo test\n"
#
# 6. Custom kittens:
#    - Write your own in Python!

# ============================================================================
# QUICK REFERENCE
# ============================================================================
# Ctrl+Shift+F1       - Show all keybindings
# Ctrl+Shift+F2       - Edit this config
# Ctrl+Shift+F5       - Reload config
# Ctrl+Shift+F12      - Browse and apply themes
# Ctrl+Shift+L        - Cycle through layouts
# Ctrl+Shift+Alt+1-7  - Go to specific layout
# Ctrl+Shift+E        - Open URLs with hints
# Ctrl+Shift+P+F      - Path hints
# Ctrl+Shift+U        - Unicode input
# Ctrl+Shift+H        - View scrollback in pager
# Ctrl+Shift+G        - View last command output
# Ctrl+Shift+Z/X      - Jump between prompts (needs shell integration)


# ============================================================================
# KITTY COMPREHENSIVE TEST CONFIGURATION
# ============================================================================
# This config showcases all major Kitty features with extensive keybindings
# Press Ctrl+Shift+F1 to see all keybindings in Kitty

# ============================================================================
# FONTS
# ============================================================================
font_family      JetBrainsMono Nerd Font
bold_font        auto
italic_font      auto
bold_italic_font auto
font_size 18.0

# Disable ligatures (or enable with "always")
disable_ligatures never

# Symbol maps - use specific fonts for specific Unicode ranges
symbol_map U+E0A0-U+E0A3,U+E0C0-U+E0C7 PowerlineSymbols

# Symbol maps - use specific fonts for specific Unicode ranges
symbol_map U+E0A0-U+E0A3,U+E0C0-U+E0C7 PowerlineSymbols

# ============================================================================
# CURSOR
# ============================================================================
cursor_shape block
cursor_blink_interval 0.5
cursor_stop_blinking_after 15.0

# ============================================================================
# SCROLLBACK
# ============================================================================
scrollback_lines 10000
scrollback_pager less --chop-long-lines --RAW-CONTROL-CHARS +INPUT_LINE_NUMBER
scrollback_pager_history_size 0
scrollback_fill_enlarged_window no
wheel_scroll_multiplier 5.0
wheel_scroll_min_lines 1
touch_scroll_multiplier 1.0

# ============================================================================
# MOUSE
# ============================================================================
mouse_hide_wait 3.0
url_color #0087bd
url_style curly
open_url_with default
url_prefixes file ftp ftps gemini git gopher http https irc ircs kitty mailto news sftp ssh
detect_urls yes
copy_on_select no
strip_trailing_spaces never
select_by_word_characters @-./_~?&=%+#
click_interval -1.0
focus_follows_mouse no
pointer_shape_when_grabbed arrow
default_pointer_shape beam
pointer_shape_when_dragging beam

# ============================================================================
# MOUSE ACTIONS - Test by using these
# ============================================================================
mouse_map left click ungrabbed mouse_handle_click selection link prompt
mouse_map shift+left click grabbed,ungrabbed mouse_handle_click selection link prompt
mouse_map ctrl+shift+left release grabbed,ungrabbed mouse_handle_click link
mouse_map ctrl+shift+left press grabbed discard_event
mouse_map middle release ungrabbed paste_from_selection
mouse_map left press ungrabbed mouse_selection normal
mouse_map ctrl+alt+left press ungrabbed mouse_selection rectangle
mouse_map left doublepress ungrabbed mouse_selection word
mouse_map left triplepress ungrabbed mouse_selection line

# ============================================================================
# PERFORMANCE TUNING
# ============================================================================
repaint_delay 10
input_delay 3
sync_to_monitor yes

# ============================================================================
# TERMINAL BELL
# ============================================================================
enable_audio_bell no
visual_bell_duration 0.0
visual_bell_color none
window_alert_on_bell yes
bell_on_tab "🔔 "
command_on_bell none
bell_path none

# ============================================================================
# WINDOW LAYOUT
# ============================================================================
remember_window_size  yes
initial_window_width  640
initial_window_height 400
enabled_layouts splits,tall,fat,grid,horizontal,vertical,stack

# Window borders and margins
window_border_width 2.0pt
draw_minimal_borders no
window_margin_width 0
single_window_margin_width -1
window_padding_width 2
placement_strategy center

# Active/inactive window colors
active_border_color #00ff00
inactive_border_color #cccccc
bell_border_color #ff5a00

# Inactive window dimming
inactive_text_alpha 0.8

# ============================================================================
# WINDOW TITLES - THE STATUS BAR FEATURE YOU ASKED ABOUT
# ============================================================================
# This controls what appears in the window title header
# Dynamic means it updates based on shell integration
dynamic_background_opacity no

# ============================================================================
# TAB BAR
# ============================================================================
tab_bar_edge top
tab_bar_margin_width 0.0
tab_bar_margin_height 0.0 0.0
tab_bar_style powerline
tab_bar_align left
tab_bar_min_tabs 2
tab_switch_strategy previous
tab_fade 0.25 0.5 0.75 1
tab_separator " ┇"
tab_powerline_style angled
tab_activity_symbol none
tab_title_template "{fmt.fg.red}{bell_symbol}{activity_symbol}{fmt.fg.tab}{title}"
active_tab_title_template none

# Tab bar colors
active_tab_foreground   #000
active_tab_background   #eee
active_tab_font_style   bold-italic
inactive_tab_foreground #444
inactive_tab_background #999
inactive_tab_font_style normal

# ============================================================================
# COLOR SCHEME
# ============================================================================
foreground #dddddd
background #000000
background_opacity 0.95
background_blur 0
background_image none
background_image_layout tiled
background_image_linear no
background_tint 0.0
background_tint_gaps 1.0
dim_opacity 0.75

# Selection colors
selection_foreground #000000
selection_background #fffacd

# The color table - standard colors
color0 #000000
color1 #cc0403
color2 #19cb00
color3 #cecb00
color4 #0d73cc
color5 #cb1ed1
color6 #0dcdcd
color7 #dddddd
color8 #767676
color9 #f2201f
color10 #23fd00
color11 #fffd00
color12 #1a8fff
color13 #fd28ff
color14 #14ffff
color15 #ffffff

# ============================================================================
# ADVANCED
# ============================================================================
shell .
editor .
close_on_child_death no
allow_remote_control yes
listen_on unix:/tmp/kitty
update_check_interval 0
startup_session none
clipboard_control write-clipboard write-primary read-clipboard-ask read-primary-ask
clipboard_max_size 512
allow_hyperlinks yes
shell_integration enabled
allow_cloning ask
clone_source_strategies venv,conda,env_var,path
term xterm-kitty

# ============================================================================
# OS SPECIFIC TWEAKS
# ============================================================================
wayland_titlebar_color system
linux_display_server auto


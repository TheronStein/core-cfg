# FZF Layout: Split Horizontal
# Preview panel below the list

FZF_LAYOUT_NAME="Split Horizontal"
FZF_LAYOUT_DESC="80% height with bottom preview"

FZF_LAYOUT_OPTS=(
    "--height=80%"
    "--layout=reverse"
    "--info=inline"
    "--preview-window=down:50%:wrap:border-top"
)

# Uses global defaults for styling

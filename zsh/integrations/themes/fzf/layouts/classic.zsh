# FZF Layout: Classic
# Simple, clean reverse layout - good for quick selections

FZF_LAYOUT_NAME="Classic"
FZF_LAYOUT_DESC="Simple 50% height, top-down list"

FZF_LAYOUT_OPTS=(
    "--height=50%"
    "--layout=reverse"
    "--info=inline"
)

# Uses global defaults for border, prompt, pointer, marker

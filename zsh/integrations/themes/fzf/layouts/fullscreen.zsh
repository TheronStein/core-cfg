# FZF Layout: Fullscreen
# Maximum screen real estate with right preview

FZF_LAYOUT_NAME="Fullscreen"
FZF_LAYOUT_DESC="Full height with 60% right preview"

FZF_LAYOUT_OPTS=(
    "--height=100%"
    "--layout=reverse"
    "--info=inline"
    "--preview-window=right:60%:wrap"
)

# Uses global defaults for styling

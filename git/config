[user]
    name = Theron Steinbrecher
    email = theron@chaoscore.org
    signingkey = E37673726444937C
[commit]
    gpgSign = true        # Note: capital S!
[tag]
    gpgSign = true
[gpg]
    program = /usr/bin/gpg    # actually not needed for pure SSH signing anymore

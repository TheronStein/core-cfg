# Vim digraphs extractor

## About

Does what it says on the tin.

## Usage

You need neovim or vim installed. This will extract the digraphs.

```sh
./digraph-extractor --output=lua
./digraph-extractor --output=json
```

or just run `./extractor`.
